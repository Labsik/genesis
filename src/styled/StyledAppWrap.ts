import { styled } from "@mui/material";

export const StyledAppWrap = styled("div")(({ theme }) => ({
    minHeight: "100vh",
    minWidth: "100vw",
    position: "relative",
    overflow: "hidden",
  }));